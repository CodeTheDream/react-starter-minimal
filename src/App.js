import React from 'react';
import Compile from "../src/containers/compile"

class App extends React.Component {
    render() {
        return (
            <Compile />
        )
    }
}

export default App;